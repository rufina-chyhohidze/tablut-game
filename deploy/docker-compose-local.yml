version: '3.7'

services:
  db:
    image: postgres:14-alpine

    volumes:
      - ~/dbs/javaTablut/data:/var/lib/postgresql/data

    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DATABASE: tablut

    ports:
      - "5432:5432"